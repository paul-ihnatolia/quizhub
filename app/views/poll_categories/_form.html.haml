= form_for @poll_category do |f|
  - if @poll_category.errors.any?
    #error_explanation
      %h2= "#{pluralize(@poll_category.errors.count, "error")} prohibited this poll_category from being saved:"
      %ul
        - @poll_category.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :string
    = f.text_field :string
  .actions
    = f.submit 'Save'
